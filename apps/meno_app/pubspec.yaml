name: meno_app
resolution: workspace
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  disco: ^1.0.3+1
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.2.4
  flutter_solidart: ^2.7.1
  go_router: ^16.3.0
  meno_auth:
    path: ../../packages/meno_auth
  meno_design_system:
    path: ../../packages/meno_design_system
  meno_domain:
    path: ../../packages/meno_domain
  meno_onboarding:
    path: ../../packages/meno_onboarding
  meno_services:
    path: ../../packages/meno_services
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
