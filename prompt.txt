So, I am wondering, since I have migrated from the pure DDD pattern and its folder structure to something more pragmatic, can the HttpServices, LocalServices and SocketServices for the different features return domain level classes and accept domain level value objects? For instance, in the broadcast feature, in the old implementation, I  had the repository impl class that would convert the dtos to domains and also return `Either` for easy error handling. But I know we said removing fpdart was acceptable since Commands handle errors well, but there is a  scenario where I am not sure commands work. In the broadcast editor manager @lib/features/broadcast/manager/broadcast_editor_manager.dart, in the `createBroadcast` command, if the get broadcast call fails, I would like to reset the creation step, but since its flutter_it anti pattern to use  try-catch blocks in commands, I cannot handle this, except with either from fpdart (if there is a way to handle this without using fpdart, please show). So, I am asking, can I make the http services return eithers (handle the error conversion) and return domain by converting from dtos to domain, and also accept domain value objects rather than  just primitive types? Is this a good idea? Since the repository impl is no longer here and we are tyring to adhere to the `flutter-architecture-expert` skill rules?